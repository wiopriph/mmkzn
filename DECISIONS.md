# DECISIONS.md — журнал решений, принятых без заказчика

Формат: `дата · что решил · почему · что перепроверить`

- 2026-08-24 · Сборка и запуск проекта — на Node v22.23.2 (nvm), а не на дефолтном v22.11.0 · oxc-parser ≥0.143 — ESM-only, а `require(esm)` работает только с Node ≥22.12; на 22.11 `nuxt build` падает («could not resolve a parseSync implementation») · зафиксировать версию Node в engines/README и на CI (Этап 1).
- 2026-08-24 · better-sqlite3 закреплён на v12 (12.11.1) вместо v13 · прекомпилированный darwin-x64 бинарник v13.0.3 стабильно падает с SIGSEGV при открытии БД на этой машине; v12 работает · при настройке CI/Docker (Этап 1) проверить, воспроизводится ли segfault v13 на linux-x64 — возможно, там можно вернуть v13.
- 2026-08-24 · Модуль nuxt-og-image отключён (`ogImage: { enabled: false }`) · требует нативный рендерер @takumi-rs/core, а OG-картинки по ТЗ — Этап 8 (только prerender) · включить и настроить на Этапе 8.
- 2026-08-24 · В package.json добавлен `pnpm.onlyBuiltDependencies: ["better-sqlite3"]` · pnpm 10 по умолчанию блокирует build-скрипты зависимостей, better-sqlite3 без этого не собирается из исходников · оставить как есть.
- 2026-08-24 · `specs[]` у «Гравий 20+» и «Щебень 40-70» — пустые массивы · на живых страницах старого сайта таблиц характеристик нет (проверено и в HTML, и в REST-контенте), а изобретать данные лаборатории запрещено ТЗ · спросить клиента, есть ли протоколы по этим двум материалам.
- 2026-08-24 · Цены трёх видов щебня оставлены `null`, на сайте выводится «по запросу» · в прайсе на главной старого сайта щебня нет (только песок ×2, ОПГС, гравий ×2) — источника цифры не существует · запросить цены щебня у клиента; пункт задачи «ни одного null в price» для щебня невыполним без выдумывания.
- 2026-08-24 · `price.unit = 'т'` у всех материалов, не «м3» из заглушки экстрактора · шапка прайса старого сайта: «Цена за безналичный расчет, руб./тн.» · —
- 2026-08-24 · `gostRef` перенесён как на сайте: «ГОСТ 8763-2014» · в заголовках таблиц старого сайта именно «87 63» (с zero-width-space внутри), хотя действующий ГОСТ на песок — 8736-2014; похоже на опечатку автора сайта, но «улучшать» перенос запрещено · спросить клиента / поправить вместе с редизайном карточек.
- 2026-08-24 · Специфика парсера: в `specs[]` экстрактор записал прайс-таблицу (единственный `<table>` на страницах), а настоящие характеристики на старом сайте сверстаны div-ами · specs всех 8 материалов перезаписаны из div-таблиц и сверены построчно против двух независимых источников (REST-контент и живой HTML) скриптом `.tmp-verify-specs.mjs` — расхождений 0 · —
- 2026-08-24 · Контент главной вынесен в `content/_data/home.yml`, файл `content/materials/glavnaya-2.md` (ложно определилась материалом из-за прайс-таблицы) удалён; страница «Наша продукция» рендерит категории компонентом из коллекции materials · схема pages в ТЗ — только `{title, seo}`, структуру секций главной в неё не уложить; коллекция `data` с passthrough-схемой подходит · при редизайне (Этап 3) решить, переезжает ли главная в `content/index.yml` как в ТЗ.
- 2026-08-24 · `alt` фотографий галереи — осмысленные, но обобщённые («портовый кран на погрузке», «отвалы материалов») · содержимое всех 12 фото с телефона установить точно нельзя, пустые alt недопустимы · уточнить подписи с клиентом при редизайне.
- 2026-08-24 · Ссылка Telegram в заглушке формы — `https://t.me/micro_cosm` · взята из соц-иконок подвала старого сайта; выглядит как личный аккаунт · подтвердить у клиента правильный канал/аккаунт до Этапа 1 (форма).
- 2026-08-24 · На новом сайте ровно один `h1` на страницу (из title); заголовки «Цены» и телефон в подвале, бывшие на старом сайте h1, понижены до h2/div · verify-copy.ts требует ровно один h1, и это правильно для SEO · —
