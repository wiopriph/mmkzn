# DECISIONS.md — журнал решений, принятых без заказчика

Формат: `дата · что решил · почему · что перепроверить`

## Этап 2 — дизайн-система (Figma «NY 90»)

- 2026-09-11 · Токены генерируются из `data/design/tokens.json` скриптом `scripts/design-tokens.ts` (`pnpm tokens`), а не напрямую из Figma Variables · Variables API доступен только на Enterprise-плане (получен 403), стили в файле не опубликованы; значения сняты с нод макета через REST API и выверены глазами · при апгрейде плана Figma заменить источник скрипта, формат tokens.css сохранится.
- 2026-09-11 · Шрифт проекта — Inter (self-host через @nuxt/fonts), Onest из заглушек убран · в макете 104 из 108 текстовых нод — Inter · Bebas Neue Cyrillic из макета (3 декоративные подписи) пока не подключён — решить при вёрстке секции «Инфраструктура».
- 2026-09-11 · Line-height'ы нормализованы в шкалу (у дизайнера один размер встречается с разными интерлиньяжами: 72/72 и 72/76.3; 24/28.8 и 24/31.4) · это разнобой макета, а не система; взяты доминирующие значения · показать дизайнеру список расхождений, писать вёрстку по токенам.
- 2026-09-11 · Отступы: базовый шаг 4px; гэпы 5/15px из макета округляются к 4/16 · в автолейаутах макета смешаны шаги 4 и 5 (24, 16, 20 против 5, 15) · проговорить с дизайнером единый шаг; счётчик arbitrary values держим ниже ~5%.
- 2026-09-11 · Реквизиты ИНН/ОГРН 1655288455/1141690017864 взяты из футера макета в contacts.yml · клиент реквизиты ещё не передавал официально, но в макете они есть · подтвердить у клиента до замены плейсхолдеров в /politika/.
- 2026-09-11 · Новый лейаут — `layouts/redesign.vue` (SiteHeader/SiteFooter); копийные страницы остаются на `layouts/default.vue` до своих этапов · редизайн выкатывается постранично, verify-copy по остальным страницам должен оставаться зелёным · удалить default.vue после Этапа 5.
- 2026-09-11 · Пункт меню «Услуги» из макета в шапку не включён; «Компания» и «Контакты» временно ведут на якоря главной (#infra, #dialog) · страниц услуг/компании/контактов ещё нет — это Этап 5 · вернуть пункты при вёрстке соответствующих страниц.
- 2026-09-11 · Иконки WhatsApp/Telegram/телефона — свои инлайн-SVG, не вырезка из макета · в экспорте макета иконки лежат единым спрайтом с клип-путями, резать дороже, визуальная разница незаметна · показать дизайнеру.

## Миграция стилей на SCSS (решение владельца проекта, 2026-09-11)

- 2026-09-11 · Tailwind v4 заменён на SCSS + `<style scoped>` (стек как в una-moliya) · доверстывать и поддерживать будет команда с опытом SCSS, а не Tailwind; свёрстана была одна страница — миграция дешевле сейчас, чем после Этапов 4-5 · PROMPT.md раздел 4.3 переписан.
- 2026-09-11 · Токены сохранены: `data/design/tokens.json` → `pnpm tokens` → `variables.scss` (CSS custom properties, имена `--color-*`/`--text-*` не менялись) + `_mixins.scss` (брейкпоинты `from-*`/`below-*` мобайл-ферст, текстовые стили `text-*`) · договорённость «значения только из токенов» теперь держится дисциплиной, а не компилятором · предложить stylelint-правило против голых hex/px (не поставлен — обсудить).
- 2026-09-11 · Lighthouse mobile после миграции: 94±1 локально (LCP ~2.6-3.0 c) против цели ≥95 · локальный замер нестабилен (CPU-троттлинг на занятой машине); добавлены format="webp" на все NuxtImg (image.format в конфиге на NuxtImg не действует) и полные ступени srcset для фоновых фото · добить метрику на проде на Этапе 8 (brotli, HTTP/2, реальный TTFB).
- 2026-09-11 · Миксины инжектируются во все `<style lang="scss">` через vite `additionalData`; в scss-файлы, подключённые через `@use` (global.scss), — явным `@use "mixins" as *` · additionalData применяется только к entry-контенту, не к @use-модулям · —

## Этап 3 — главная

- 2026-09-11 · SEO title/description главной сохранены прежними (байт в байт из Yoast), тексты секций — новые из макета · мета переписывается только осознанным решением (PROMPT 8.2), макет мету не описывает · когда решим переписать мету главной — синхронизировать эталонную запись `/` в верификации.
- 2026-09-11 · Цифры компании в секциях «Инфраструктура» (32/94 Га, 500+ т, 90+ ед.) и «Нам доверяют» (15 лет, 940 т) взяты из макета как есть · других источников нет; выдумывать нельзя, макет — единственное согласованное с клиентом место · ОБЯЗАТЕЛЬНО подтвердить цифры у клиента до продакшн-деплоя главной.
- 2026-09-11 · Опечатки макета в вёрстку не перенесены: «Едениц»→«Единиц», «закачика»→«заказчика», «РЕСПУБЛИУКА»→«Республика» · очевидные тайпо, не редактура текста · сообщить дизайнеру.
- 2026-09-11 · Цены в карточках продукции — из карточек материалов (usePrices), а не цифры макета (там 1300/1500/1250/450 — заглушки дизайнера) · цена живёт в одном месте (PROMPT, п.1 продуктовых требований) · дизайнеру не нужно править цены в макете.
- 2026-09-11 · Секция «Новости» свёрстана, но рендерится только при непустой коллекции articles; сейчас скрыта, пункт «Новости» в футере убран до Этапа 7 · заголовки новостей в макете — вымышленные заглушки, публиковать их нельзя; пустая лента хуже отсутствия (PROMPT 8.4) · спросить клиента, какие реальные события публикуем на старте.
- 2026-09-11 · Форма «Приглашаем к диалогу» свёрстана полностью, но POST /api/lead — заглушка с 501: форма показывает телефонный фолбэк · бэкенд формы (Postgres+Telegram) — Этап 1, до деплоя это не мешает · подключить настоящий обработчик на Этапе 1, до переключения DNS.
- 2026-09-11 · Кнопка «Подробнее о нас» из секции «Инфраструктура» и стрелка из карточки сертификатов не свёрстаны · вести некуда — страницы «Компания» и «Документы» появятся на Этапе 5 · вернуть вместе со страницами.
- 2026-09-11 · Кнопка play в hero (видео) не свёрстана — видеоролика не существует · в макете есть видеоблок, контента нет · спросить клиента про видео.
- 2026-09-11 · Подписи городов на карте «Снабжаем» (Болгар, Аксубаево, Альметьевск) не перенесены — карта без текстовых меток · в SVG-экспорте карты подписи не попали (текстовые ноды отдельно), а список «важных проектов» должен подтвердить клиент · вернуться при наполнении секции реальной географией.

- 2026-08-24 · Сборка и запуск проекта — на Node v22.23.2 (nvm), а не на дефолтном v22.11.0 · oxc-parser ≥0.143 — ESM-only, а `require(esm)` работает только с Node ≥22.12; на 22.11 `nuxt build` падает («could not resolve a parseSync implementation») · зафиксировать версию Node в engines/README и на CI (Этап 1).
- 2026-08-24 · better-sqlite3 закреплён на v12 (12.11.1) вместо v13 · прекомпилированный darwin-x64 бинарник v13.0.3 стабильно падает с SIGSEGV при открытии БД на этой машине; v12 работает · при настройке CI/Docker (Этап 1) проверить, воспроизводится ли segfault v13 на linux-x64 — возможно, там можно вернуть v13.
- 2026-08-24 · Модуль nuxt-og-image отключён (`ogImage: { enabled: false }`) · требует нативный рендерер @takumi-rs/core, а OG-картинки по ТЗ — Этап 8 (только prerender) · включить и настроить на Этапе 8.
- 2026-08-24 · В package.json добавлен `pnpm.onlyBuiltDependencies: ["better-sqlite3"]` · pnpm 10 по умолчанию блокирует build-скрипты зависимостей, better-sqlite3 без этого не собирается из исходников · оставить как есть.
- 2026-08-24 · `specs[]` у «Гравий 20+» и «Щебень 40-70» — пустые массивы · на живых страницах старого сайта таблиц характеристик нет (проверено и в HTML, и в REST-контенте), а изобретать данные лаборатории запрещено ТЗ · спросить клиента, есть ли протоколы по этим двум материалам.
- 2026-08-24 · Цены трёх видов щебня оставлены `null`, на сайте выводится «по запросу» · в прайсе на главной старого сайта щебня нет (только песок ×2, ОПГС, гравий ×2) — источника цифры не существует · запросить цены щебня у клиента; пункт задачи «ни одного null в price» для щебня невыполним без выдумывания.
- 2026-08-24 · `price.unit = 'т'` у всех материалов, не «м3» из заглушки экстрактора · шапка прайса старого сайта: «Цена за безналичный расчет, руб./тн.» · —
- 2026-08-24 · `gostRef` перенесён как на сайте: «ГОСТ 8763-2014» · в заголовках таблиц старого сайта именно «87 63» (с zero-width-space внутри), хотя действующий ГОСТ на песок — 8736-2014; похоже на опечатку автора сайта, но «улучшать» перенос запрещено · спросить клиента / поправить вместе с редизайном карточек.
- 2026-08-24 · Специфика парсера: в `specs[]` экстрактор записал прайс-таблицу (единственный `<table>` на страницах), а настоящие характеристики на старом сайте сверстаны div-ами · specs всех 8 материалов перезаписаны из div-таблиц и сверены построчно против двух независимых источников (REST-контент и живой HTML) скриптом `.tmp-verify-specs.mjs` — расхождений 0 · —
- 2026-08-24 · Контент главной вынесен в `content/_data/home.yml`, файл `content/materials/glavnaya-2.md` (ложно определилась материалом из-за прайс-таблицы) удалён; страница «Наша продукция» рендерит категории компонентом из коллекции materials · схема pages в ТЗ — только `{title, seo}`, структуру секций главной в неё не уложить; коллекция `data` с passthrough-схемой подходит · при редизайне (Этап 3) решить, переезжает ли главная в `content/index.yml` как в ТЗ.
- 2026-08-24 · `alt` фотографий галереи — осмысленные, но обобщённые («портовый кран на погрузке», «отвалы материалов») · содержимое всех 12 фото с телефона установить точно нельзя, пустые alt недопустимы · уточнить подписи с клиентом при редизайне.
- 2026-08-24 · Ссылка Telegram в заглушке формы — `https://t.me/micro_cosm` · взята из соц-иконок подвала старого сайта; выглядит как личный аккаунт · подтвердить у клиента правильный канал/аккаунт до Этапа 1 (форма).
- 2026-08-24 · Карточки материалов рендерит один динамический роут `app/pages/[slug].vue` (поиск по `legacyPath`), а не 8 отдельных файлов · слаги уйдут на Этапе 4 вместе с ЧПУ, дублировать 8 одинаковых страниц незачем; явные `nasha-produkcziya.vue` и `politika.vue` имеют приоритет над динамикой · при переходе на ЧПУ роут переедет в `produkcziya/[slug].vue`.
- 2026-08-24 · `/politika/` собрана как типовая политика по 152-ФЗ; ИНН, ОГРН и юридический адрес — плейсхолдеры `{{ИНН}}`, `{{ОГРН}}`, `{{ЮРИДИЧЕСКИЙ_АДРЕС}}`, страница под `noindex` · реквизиты юрлица клиент ещё не передал (раздел 13 PROMPT.md) · заменить плейсхолдеры и снять noindex, как только придут реквизиты.
- 2026-08-24 · Форма заявки — заглушка: телефон + WhatsApp + Telegram (ссылки со старого сайта) · формы, БД и Telegram-бот — Этап 1 по ТЗ · —
- 2026-08-24 · На новом сайте ровно один `h1` на страницу (из title); заголовки «Цены» и телефон в подвале, бывшие на старом сайте h1, понижены до h2/div · verify-copy.ts требует ровно один h1, и это правильно для SEO · —
